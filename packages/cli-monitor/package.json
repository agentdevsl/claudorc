{
  "name": "@agentpane/cli-monitor",
  "version": "0.1.0",
  "description": "CLI monitor daemon for AgentPane — watches Claude Code sessions in real-time",
  "type": "module",
  "bin": {
    "cli-monitor": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build --compile --outfile=dist/cli-monitor src/index.ts",
    "build:all": "bun build --compile --target=bun-darwin-arm64 --outfile=dist/cli-monitor-darwin-arm64 src/index.ts && bun build --compile --target=bun-darwin-x64 --outfile=dist/cli-monitor-darwin-x64 src/index.ts && bun build --compile --target=bun-linux-x64 --outfile=dist/cli-monitor-linux-x64 src/index.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest"
  }
}
