{
  "name": "@agentpane/cli-monitor",
  "version": "0.1.1",
  "description": "CLI monitor daemon for AgentPane — watches Claude Code sessions in real-time",
  "type": "module",
  "bin": {
    "cli-monitor": "dist/index.js"
  },
  "files": [
    "dist/index.js"
  ],
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build --compile --define \"PKG_VERSION='$(node -p \"require('./package.json').version\")'\" --outfile=dist/cli-monitor src/index.ts",
    "build:js": "bun build --target=node --define \"PKG_VERSION='$(node -p \"require('./package.json').version\")'\" --outfile=dist/index.js src/index.ts",
    "build:all": "bun build --compile --define \"PKG_VERSION='$(node -p \"require('./package.json').version\")'\" --target=bun-darwin-arm64 --outfile=dist/cli-monitor-darwin-arm64 src/index.ts && bun build --compile --define \"PKG_VERSION='$(node -p \"require('./package.json').version\")'\" --target=bun-darwin-x64 --outfile=dist/cli-monitor-darwin-x64 src/index.ts && bun build --compile --define \"PKG_VERSION='$(node -p \"require('./package.json').version\")'\" --target=bun-linux-x64 --outfile=dist/cli-monitor-linux-x64 src/index.ts",
    "test": "vitest run",
    "prepublishOnly": "bun run test && bun run build:js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/agentdevsl/claudorc.git",
    "directory": "packages/cli-monitor"
  },
  "bugs": {
    "url": "https://github.com/agentdevsl/claudorc/issues"
  },
  "homepage": "https://github.com/agentdevsl/claudorc/tree/main/packages/cli-monitor#readme",
  "author": "AgentPane",
  "license": "MIT",
  "keywords": [
    "cli",
    "monitor",
    "claude",
    "agent",
    "agentpane",
    "daemon"
  ],
  "engines": {
    "node": ">=22.0.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "chokidar": "^5.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
